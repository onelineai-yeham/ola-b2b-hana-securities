apiVersion: v1
kind: Secret
metadata:
  name: hana-news-api-secret
  namespace: default
type: Opaque
stringData:
  # Silver DB credentials
  SILVER_DB_USER: "ola_b2b_hana_securities"
  SILVER_DB_PASSWORD: "CHANGE_ME"  # Update with actual password
  
  # Gold DB credentials
  GOLD_DB_USER: "hana_news_api"
  GOLD_DB_PASSWORD: "CHANGE_ME"  # Update with actual password
